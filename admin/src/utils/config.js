const devConfig = {
    baseUrl: 'http://localhost:9000/admin/api',
    environment: 'dev'
}
const proConfig = {
    baseUrl: 'http://localhost:9000/admin/api',
    environment: 'pro'
}

export {devConfig, proConfig}